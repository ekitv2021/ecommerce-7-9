export interface Product {
    id: number,
    name: string,
    price: number,
    code: string,
    image: string,
    isAvailable: boolean,
    manufacturedDate: string,
    description?: string
    
}
